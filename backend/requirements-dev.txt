# Dépendances de test uniquement (voir backend/tests/). Pas nécessaires en
# production -- installez avec `pip install -r requirements-dev.txt` (qui
# tire aussi requirements.txt via la ligne ci-dessous).
-r requirements.txt

pytest>=8.0.0
httpx>=0.27.0
